{
    "routes": [
        {
            "route": "/",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/login",
            "redirect": "/.auth/login/github"
        }
    ],
    "responseOverrides": {
        "401": {
            "rewrite": "/.auth/login/github",
            "statusCode": 200
        }
    }
}