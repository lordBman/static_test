{
    "routes": [
        {
            "route": "/",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/login",
            "rewrite": "/.auth/login/github"
        },
        {
            "route": "/logout",
            "rewrite": "/.auth/logout/github"
        }
    ],
    "responseOverrides": {
        "401": {
            "rewrite": "/.auth/login/github",
            "statusCode": 200
        }
    }
}